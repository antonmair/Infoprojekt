point_positions_angles
  -input point_positions_list (list of all current points) 
  -point_positions_angles (given list is split into pairs of nearest three points)

angle_calc (unfinished)
  -input point_positions_angles
  -output angle_calc (calculate which angles have what values)

angle_punishment (unfinished)
  -input angle_calc
  -output angle_punishment (positive decimal number indicating deviation of set path)


